# Project title         :  Pay Book – Billing and Invoice Generator
# Project for           :  IT Fundamentals and Applications
# Project completed by  :  Muhammad Abdullah Elahi, TC-061
# Project submitted to  :  Dr. Amir Zeb, Lab Instructor and Class Teacher

from tabulate import tabulate


def pretty_print_details(values):
    # ────────────────────────────────────────────────
    #  Start → Print Header → "PROJECT BILL SUMMARY"
    #  (Green → Purple → Yellow block in flowchart)
    # ────────────────────────────────────────────────
    print("\n========================")
    print("📘  PROJECT BILL SUMMARY")
    print("========================\n")

    # ────────────────────────────────────────────────
    #  Print General Information Section
    #  (Orange block: If key exists → print key/value)
    # ────────────────────────────────────────────────
    general_info_keys = ["Project Name",
                         "Client Name", "Date", "Objective", "Time"]

    for key in general_info_keys:
        if key in values:  # Diamond decision in flowchart
            print(f"{key:<15}: {values[key]}")  # Blue block → print key:value

    # ────────────────────────────────────────────────
    #  Print "Services Provided" Section Header
    #  (Pink Block)
    # ────────────────────────────────────────────────
    print("\n-----------------------")
    print("🧾 Services Provided")
    print("-----------------------\n")

    # ────────────────────────────────────────────────
    #  Decision: "Services" exist and not empty?
    #  (Purple diamond block)
    # ────────────────────────────────────────────────
    if "Services" in values and len(values["Services"]) > 0:

        # ────────────────────────────────────────────────
        #  Create Table Rows for Services
        #  (Pink blocks: create rows + define headers)
        # ────────────────────────────────────────────────
        table = [
            [
                i + 1,
                s["Service/ Product"],
                f"{s['Amount (PKR)']:,.2f}",
                f"{s['Discount (%)']:,.2f}",
                f"{s['Total (PKR)']:,.2f}"
            ]
            for i, s in enumerate(values["Services"])
        ]

        headers = ["#", "Service/ Product",
                   "Amount (PKR)", "Discount (%)", "Total (PKR)"]

        # ────────────────────────────────────────────────
        #  Print Table Using Tabulate (Fancy Grid Format)
        #  (Pink: "Print Services Table")
        # ────────────────────────────────────────────────
        print(tabulate(table, headers, tablefmt="fancy_grid"))

    else:
        # ────────────────────────────────────────────────
        #  If no services → print message
        #  (Pink → Left branch)
        # ────────────────────────────────────────────────
        print("No services added.\n")

    # ────────────────────────────────────────────────
    #  Print Billing Summary Header
    #  (Purple block in bottom section)
    # ────────────────────────────────────────────────
    print("\n-----------------------")
    print("💰 Billing Summary")
    print("-----------------------\n")

    # ────────────────────────────────────────────────
    #  Print Billing Details
    #  (Gold block in flowchart)
    # ────────────────────────────────────────────────
    print(f"Total Amount (PKR): {values['Total Amount (PKR)']:,.2f}")
    print(f"Advance (50%): {values['Advance']:,.2f}")
    print(
        f"Remaining Balance: {values['Total Amount (PKR)'] - values['Advance']:,.2f}")

    # ────────────────────────────────────────────────
    #  Print Confirmation Message → End
    #  (Gold block → Green End block)
    # ────────────────────────────────────────────────
    print("\n✅ Invoice data successfully generated!\n")
